

## Plan: Save Project Context to Memory

Since no UI work is requested yet, the only action is to persist the product definition to project memory so every future session has access to it.

### Files to create:
1. **mem://index.md** — Core rules and memory index
2. **mem://features/product-context** — Full product definition (users, flows, entities, principles)
3. **mem://design/design-system** — Design principles and visual direction

### What gets saved:
- Product name and purpose
- Target users and their roles
- Core jobs, primary flows, key entities
- Design principles and system mindset
- Instruction to build screen-by-screen only

No code changes. Waiting for explicit screen instructions before generating any UI.

